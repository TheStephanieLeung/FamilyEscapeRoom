import Phaser from 'phaser';

const config = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  parent: 'game-container',
  scene: {
    preload: function() {
    },
    create: function() {
    },
    update: function() {
    }
  }
};

export default config;
